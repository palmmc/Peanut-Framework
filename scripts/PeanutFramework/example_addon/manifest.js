import { Manifest } from "../Classes/manifest";
new Manifest({
    //dependencies: { server: { beta: true }, "server-ui": {} },
    //modules: { scripts: {} },
    metadata: { authors: ["palm1"] },
}).compile();
